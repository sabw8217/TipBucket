<%= form_for(@tip) do |f| %>
  <%= f.label :location, "location" %><%= f.text_field :location %>
  <%= f.text_field :crime_type %>
  <%= f.check_box :has_occurred %>
  <% if @tip.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tip.errors.count, "error") %> prohibited this tip from being saved:</h2>

      <ul>
      <% @tip.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
